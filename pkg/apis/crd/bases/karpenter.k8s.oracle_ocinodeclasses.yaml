---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: ocinodeclasses.karpenter.k8s.oracle
spec:
  group: karpenter.k8s.oracle
  names:
    categories:
      - karpenter
    kind: OciNodeClass
    listKind: OciNodeClassList
    plural: ocinodeclasses
    shortNames:
      - ocinc
      - ocincs
    singular: ocinodeclass
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: OciNodeClass is the Schema for the OciNodeClass API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              properties:
                blockDevices:
                  items:
                    properties:
                      sizeInGBs:
                        format: int64
                        type: integer
                      vpusPerGB:
                        format: int64
                        type: integer
                    required:
                      - sizeInGBs
                      - vpusPerGB
                    type: object
                  type: array
                bootConfig:
                  properties:
                    bootVolumeSizeInGBs:
                      format: int64
                      type: integer
                    bootVolumeVpusPerGB:
                      format: int64
                      type: integer
                  required:
                    - bootVolumeSizeInGBs
                    - bootVolumeVpusPerGB
                  type: object
                image:
                  properties:
                    compartmentId:
                      type: string
                    id:
                      type: string
                    name:
                      type: string
                  type: object
                imageFamily:
                  type: string
                instanceShapeAds:
                  items:
                    type: string
                  type: array
                launchOptions:
                  properties:
                    bootVolumeType:
                      description: Emulation type for the boot volume. * `ISCSI` - ISCSI
                        attached block storage device. * `SCSI` - Emulated SCSI disk.
                        * `IDE` - Emulated IDE disk. * `VFIO` - Direct attached Virtual
                        Function storage. This is the default option for local data
                        volumes on platform images. * `PARAVIRTUALIZED` - Paravirtualized
                        disk. This is the default for boot volumes and remote block
                        storage volumes on platform images.
                      type: string
                    firmware:
                      description: Firmware used to boot VM. Select the option that
                        matches your operating system. * `BIOS` - Boot VM using BIOS
                        style firmware. This is compatible with both 32 bit and 64 bit
                        operating systems that boot using MBR style bootloaders. * `UEFI_64`
                        - Boot VM using UEFI style firmware compatible with 64 bit operating
                        systems. This is the default for platform images.
                      type: string
                    isConsistentVolumeNamingEnabled:
                      description: Whether to enable consistent volume naming feature.
                        Defaults to false.
                      type: boolean
                    networkType:
                      description: Emulation type for the physical network interface
                        card (NIC). * `E1000` - Emulated Gigabit ethernet controller.
                        Compatible with Linux e1000 network driver. * `VFIO` - Direct
                        attached Virtual Function network controller. This is the networking
                        type when you launch an instance using hardware-assisted (SR-IOV)
                        networking. * `PARAVIRTUALIZED` - VM instances launch with paravirtualized
                        devices using VirtIO drivers.
                      type: string
                    remoteDataVolumeType:
                      description: Emulation type for volume. * `ISCSI` - ISCSI attached
                        block storage device. * `SCSI` - Emulated SCSI disk. * `IDE`
                        - Emulated IDE disk. * `VFIO` - Direct attached Virtual Function
                        storage. This is the default option for local data volumes on
                        platform images. * `PARAVIRTUALIZED` - Paravirtualized disk.
                        This is the default for boot volumes and remote block storage
                        volumes on platform images.
                      type: string
                  type: object
                preInstallScript:
                  type: string
                securityGroupNames:
                  items:
                    type: string
                  type: array
                subnetName:
                  type: string
                tags:
                  additionalProperties:
                    type: string
                  type: object
                userData:
                  type: string
                vcnId:
                  type: string
              required:
                - bootConfig
                - image
                - imageFamily
                - instanceShapeAds
                - subnetName
                - vcnId
              type: object
            status:
              properties:
                image:
                  properties:
                    compartmentId:
                      type: string
                    id:
                      type: string
                    name:
                      type: string
                  type: object
                subnets:
                  items:
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                  type: array
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
